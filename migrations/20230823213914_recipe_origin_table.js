/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function (knex) {
  return knex.schema.createTable("recipes_origins", function (table) {
    table.integer("recipes_id").unsigned().references("recipes.id");
    table.integer("origins_id").unsigned().references("origins.id");
  });
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function (knex) {
  return knex.schema.dropTable("recipes_origins");
};
